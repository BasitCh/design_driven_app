import 'package:flutter/material.dart';

class ContactErrorWidget extends StatelessWidget {
  const ContactErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
